apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: java-microservices
spec:
  selector:
    app: product-service
  ports:
    - port: 8083
      targetPort: 8080
  type: ClusterIP